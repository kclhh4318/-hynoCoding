#include <iostream>
#include <vector>
using namespace std;

void def(){
    cin.tie(0); cout.tie(0);
    ios_base::sync_with_stdio(false);
}

int GCD(int a, int b){
    if(b == 0) return a;
    else return GCD(b, a % b);
}

int main(){

    int N;

    return 0;
}